name: org_parser
description: A pure-Dart parser for Emacs Org Mode (https://orgmode.org) markup
version: 5.6.0
homepage: https://github.com/amake/org_parser

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  petitparser: ^6.0.1
  functional_zipper: ^0.0.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.12.0
  collection: ^1.0.0
